# Data directory

Place optional local media at `data/raw/sample.mp4`. That path is gitignored.

Do not commit:

- parking-lot footage
- datasets
- model weights
- databases
- generated annotated videos

Tests and the default demo use deterministic synthetic media generated at runtime.
